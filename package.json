{
  "name": "delta-mcp-prototype",
  "version": "1.0.1",
  "author": "Stanislav Panchenko",
  "description": "MCP wrapper for the UMT API",
  "scripts": {
    "build": "esbuild index.js --bundle --platform=node --format=esm --banner:js=\"#!/usr/bin/env node\" --outfile=dist/index.js",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@toon-format/toon": "^2.1.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "prettier": "^3.9.4"
  },
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "delta-mcp-prototype": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "license": "ISC"
}
